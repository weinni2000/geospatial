<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">
    <t t-name="base_geoengine.DomainSelectorGeoFieldInput">
        <div class="d-flex align-items-center">
            <ModelSelector
                value="this.state.resModel"
                onModelSelected.bind="this.onModelChange"
            />
            <div t-if="this.state.resModel.length > 0" class="col-5">
                <ModelFieldSelector
                    fieldName="this.state.fieldName"
                    resModel="this.state.resModel"
                    readonly="false"
                    update="(name) => this.onFieldModelChange(name)"
                    isDebugMode="false"
                />
            </div>
            <button class="btn btn-link col-1" t-on-click.prevent="this.display">
                <i class="fa fa-pencil-square" />
            </button>
        </div>
    </t>
</templates>
